services:
  redis:
    image: redis:8-alpine
    command: redis-server --port 16399
    ports:
      - "127.0.0.1:16399:16399"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -p 16399 ping | grep -q PONG"]
      interval: 1s
      timeout: 3s
      retries: 10
