[project]
name = "django-redis-tasks-demo"
version = "0.0.1"
description = "Runnable Django dashboard for the django-redis-tasks Redis backend"
requires-python = ">=3.14"
dependencies = [
    "django-redis-tasks",
]

[tool.uv.sources]
django-redis-tasks = { path = "..", editable = true }
